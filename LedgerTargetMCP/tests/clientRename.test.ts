// DRAFT scaffold only — no executable target MCP tests exist here yet.
//
// Planned tests cover Swift byte/hash parity at 0, 2^53-1, 2^53, signed-bigint
// maximum and UInt64 maximum; authenticated-context scope; malformed decimal,
// negative, overflow and alternate JSON forms; exact RPC text; applied/rejected
// result binding; cross-Account refusal; absence of service-role credentials;
// and stable privacy-safe telemetry with no Client name or command bytes. The
// future suite must also cover the approved O-043 whitespace/control/NUL,
// Unicode and UTF-8-size fixtures and reject unknown terminal codes.
