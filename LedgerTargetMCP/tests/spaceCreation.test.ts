// DRAFT scaffold only: no executable MCP tests exist here.
//
// READY must require Swift/TypeScript canonical-byte and fingerprint parity for
// Project and Business Inventory commands, nil/normalized notes, Unicode and
// duplicate names, scoped-user credentials, exact terminal identity, bounded
// failures, and absence of service-role, generic CRUD, or payload logging paths.
