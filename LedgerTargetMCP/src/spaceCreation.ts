// DRAFT scaffold only: no MCP tool or Supabase call exists here.
//
// The READY implementation may define one gated create_space target tool using
// the same create-space-v1 canonical command bytes, fingerprint, scoped-user RPC,
// and terminal-result validation as Swift. Account/Principal scope must come from
// authenticated context; no service-role credential, provider-specific identity,
// raw command log, generic CRUD field map, or unrelated Space behavior is allowed.
