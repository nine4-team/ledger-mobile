import { View } from 'react-native';

// Dummy screen required by Expo Router for the Add tab.
// The Add tab intercepts presses to show a bottom sheet instead of navigating here.
export default function AddScreen() {
  return <View />;
}
