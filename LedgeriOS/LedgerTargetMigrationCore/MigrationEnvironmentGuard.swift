// READY SCAFFOLD ONLY — no executable migration environment guard exists here.
//
// A future READY implementation may define only a provider-free preflight
// primitive
// that revalidates one canonical MigrationRunPlan against one exact validated
// target binding, Account-scope hash, fixture identity, artifact/contract set,
// requested mode, and non-secret credential descriptors before yielding an
// package-visible, private-initializer, non-Codable process-local consistency
// token. A later same-package sole-executor/composition slice must make
// provider construction require that token; this primitive cannot prove that
// integration ordering by itself and must independently match trusted policy.
//
// The initial admissible tuple is source_fixture + targetLocal + dry_run + no
// source credential + no target credential. Apply, source production, target
// staging/production, provider construction, migration execution, and authority
// activation remain unimplemented and unauthorized.
