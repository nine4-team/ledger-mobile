// DRAFT scaffold only: no executable provider tests exist here.
//
// READY must require encrypted offline acceptance/restart for both scopes,
// exact replay, duplicate names, FIFO upload, transient retention, durable
// rejection isolation, membership loss, malformed/foreign/stale result refusal,
// and authoritative readback before optimism clears. Tests must also prove the
// pending Space appears immediately in the exact destination directory as
// partial evidence and cannot leak into another Account or scope.
