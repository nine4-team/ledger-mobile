// READY scaffold only — the executable provider test suite is absent.
//
// Required deterministic coverage: shared generic and exact account-bound Client
// archive identity; command/version/fingerprint and UInt64 boundaries; atomic
// encrypted acceptance/replay/restart; pending Client creation and accepted
// Project setup before archive FIFO; queued/applying dependency wait, rejected
// Client-creation unblocking toward authoritative terminal archive rejection,
// overlay retention until that result, rejected Project-setup
// unblocking and applied Project preservation; immediate Client directory/detail Active-to-
// Archived projection for every consumer; Project Setup option removal and new/
// unaccepted stale/current post-archive Project-creation refusal; exact replay of
// a Project setup accepted before archive resolves before overlay validation; no
// false empty/ready authority;
// transient retention; applying/applied/rejected observation; exact result linkage;
// rejected-overlay rollback; new-ID/refreshed-revision retry; applied waits for
// authoritative archived/newer readback; stale readback retention; no resurrection;
// malformed evidence/result and unknown-code refusal; Account/Principal mismatch;
// cancellation/termination/runtime-close drainage; and byte-for-byte preservation
// of Client identity/name/creation audit, every currently represented Project and
// related/history row; each later table must add a repeated preservation regression. Tests
// use only disposable encrypted databases and synthetic identities.
