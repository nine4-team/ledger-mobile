// DRAFT scaffold only: no executable Space-browser provider tests exist here.
//
// A later READY implementation must use a real encrypted local database plus a
// controlled exact-scope completeness source to prove Project and Business-
// Inventory isolation, active-only filtering, duplicate-name-safe stable
// identity, frozen deterministic order, cached partial/stale truth, false-empty
// refusal, both row/completion startup orders, membership loss/reactivation,
// cancellation, scope replacement, close drainage, and encrypted restart.
//
// The suite must also prove that selection derives one existing exact-Account /
// exact-Space core-details request, that late list or detail evidence cannot
// rebound a replaced selection, and that malformed, duplicate, cross-Account,
// cross-scope, inactive-membership, overflow, or failed local evidence yields no
// mixed-trust snapshot and no leaked provider detail. Checklist progress must be
// complete; unavailable Item counts must be omitted rather than rendered as zero.
// Hosted Sync/Auth and every mutation remain outside this scaffold.
