// READY scaffold only — no executable tests.
//
// The implementation suite will prove exact zero and each queued, applying,
// unresolved-rejected, and unverified-attachment class; terminal operation
// exclusion; pending/missing/corrupt attachment inclusion; malformed and
// cross-scope refusal; queue-free staging/final vault orphan refusal across
// restart; ps_crud divergence; stable unchanged observations; monotonic
// same-count identity transitions; encrypted restart durability; mutations
// after the apparent stable read and during journal commit; two concurrent
// callers; and fail-closed database, vault, and observation-journal failures.
//
// It will also exercise the existing SessionEndPolicy with the provider's
// summary without deleting data, signing out, claiming remote application, or
// invoking hosted Supabase, PowerSync, Firebase, migration, or cutover paths.
