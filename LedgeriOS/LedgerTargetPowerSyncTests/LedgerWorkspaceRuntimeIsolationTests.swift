// READY scaffold only — no executable tests.
//
// The implementation suite will prove identical validated bindings reopen the
// same encrypted database while environment, bundle, persistence-relevant
// manifest, Principal and Account changes cannot collide. It will cover
// adversarial valid identifiers, invalid local namespace prefixes, isolation
// after a changed persistence binding, failure ordering
// before Keychain/database mutation, ordinary close/reopen durability, and the
// absence of destructive close, Auth, hosted Sync or Firebase behavior.
