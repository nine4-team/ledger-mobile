// READY scaffold only: no executable staging adapter exists here yet.
//
// The bounded implementation will forward one typed source Project through the
// existing Account-bound runtime and retain no second query, eligibility rule,
// cache, subscription, selection authority, or write path. Replacement,
// cancellation, and workspace close must drain the forwarded observation.
