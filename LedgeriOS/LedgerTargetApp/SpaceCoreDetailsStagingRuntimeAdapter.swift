// READY scaffold — no executable runtime adapter exists at this checkpoint.
//
// The future thin adapter may forward only the typed Account-bound exact-Space
// watch exposed by LedgerOfflineClientRuntime. It must expose no database, SQL,
// subscription, credential, authorization, mutation, or provider implementation.
