// READY scaffold only — no SwiftUI destination picker exists here.
//
// The implementation may add one isolated staging-only picker that renders the
// tested presenter/AppModel's local readiness/empty/failure state, lists active
// same-scope Space rows in canonical order, and submits user selection by stable
// SpaceID rather than display text or row position. Selection is presentation
// evidence only; this view does not define final production retention policy.
//
// The view has no Save/Assign/Clear action, raw Space editor, Item selection,
// MCP entry point, hosted integration, migration, production or cutover claim.
