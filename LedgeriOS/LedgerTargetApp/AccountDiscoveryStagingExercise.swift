// READY scaffold only. Planned isolated staging exercise:
// - render waiting, partial, stale, ready-empty and ready-with-Accounts states;
// - show only visibility-safe Account ID/display-name rows;
// - allow an explicit row action to create and validate a WorkspaceSelectionIntent;
// - never auto-select the first or remembered Account;
// - label selection as local intent rather than current server authorization;
// - expose only stable non-secret diagnostics; and
// - perform no workspace activation, hosted Auth/Sync, migration or production work.
