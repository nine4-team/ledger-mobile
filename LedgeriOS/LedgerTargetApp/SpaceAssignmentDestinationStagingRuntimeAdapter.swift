// READY scaffold only — no runtime adapter exists here.
//
// The implementation may add one thin Account-bound adapter that forwards an
// exact typed Project-or-Business-Inventory destination watch through the
// lifecycle-owned target runtime. It exposes no database, SQL, completeness
// source, Account rebinding, provider choice, mutation port or authorization
// decision and owns no independent readiness or ordering semantics.
