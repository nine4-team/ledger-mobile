// DRAFT placeholder: adapt the future Account-bound offline runtime read stream to the
// Core-only vendor-suggestion picker AppModel without exposing PowerSync or authorization.
// Excludes mutation adapters, default seeding, and every O-026 write.
