// DRAFT scaffold only: no executable Space-browser staging composition exists.
//
// A later READY implementation may compose the Core-only Space-browser AppModel
// against the existing Account-bound close-aware target runtime and render one
// isolated Project-or-Business-Inventory active-list-to-existing-detail flow.
// The view must remain a thin renderer over tested state and stable-ID actions,
// label cached/incomplete evidence honestly, omit unavailable Item counts rather
// than render zero, and present no false empty without exact list completion.
// Archived Space authority remains available through exact detail; this list
// flow does not invent an archived section.
//
// This scaffold adds no target-project membership, production route, provider
// import, database/SQL/subscription/credential surface, mutation control, hosted
// Sync/Auth connection, Firebase dependency, migration, release, or cutover.
