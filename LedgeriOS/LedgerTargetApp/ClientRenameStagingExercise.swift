// DRAFT scaffold only — no SwiftUI or target-app wiring exists here yet.
//
// The implementation may add one isolated staging-only Client rename exercise
// that watches the exact Client/revision, invokes ClientRenameUseCase, displays
// queued/partial/conflict state immediately, and demonstrates that an existing
// Project read resolves the new current Client name without changing Project
// revision. It must preserve the permanent LOCAL SPIKE / NO HOSTED SERVICES
// boundary and makes no production-quality Client-management UI claim. O-042
// and O-043 must be approved before executable rename behavior is added.
