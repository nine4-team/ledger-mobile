// DRAFT scaffold only: no executable Space-browser provider or runtime exists here.
//
// A later READY implementation may add one Account-bound, exact Project-or-
// Business-Inventory Space-browser query over the existing encrypted local
// Space projection. It must consume the separately frozen Space-browser Core
// contracts and reuse the existing Space-assignment destination and exact-Space
// detail implementations rather than create a third Space identity, scope,
// lifecycle, revision, readiness, ordering, or detail model.
//
// The application list presents active rows only, applies the frozen deterministic
// order, and distinguishes waiting, incomplete
// partial/cached, stale, ready nonempty, authoritative empty, and bounded failure.
// Zero local rows cannot prove empty without active exact-Account membership and
// fresh completion evidence for this exact current-process list subscription.
// Existing assignment-destination streams do not carry complete checklist
// evidence and therefore cannot establish browser completeness. Archived Space
// authority remains available through exact detail; this slice invents no
// archived list presentation.
//
// Consumer cancellation, scope replacement, membership loss, and workspace
// close must unsubscribe, cancel, and join every list observer before the
// encrypted database closes. Reopen may reuse cached rows but must reset current-
// process completion. No provider, SQL, database, subscription, credential, or
// authorization type may escape the package-owned runtime facade. No mutation,
// hosted Sync/Auth wiring, Firebase access, production route, or cutover is
// authorized by this scaffold.
