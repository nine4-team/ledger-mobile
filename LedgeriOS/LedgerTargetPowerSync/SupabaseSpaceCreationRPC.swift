// DRAFT scaffold only: no executable Supabase call exists here.
//
// The READY implementation may send the exact canonical create-space-v1 command
// text through one scoped-user RPC and validate the immutable terminal result.
// Account and Principal come from the authenticated workspace/session, never a
// service-role credential. Provider errors must map to bounded shared operation
// outcomes without logging Space text, canonical payloads, tokens, or keys.
