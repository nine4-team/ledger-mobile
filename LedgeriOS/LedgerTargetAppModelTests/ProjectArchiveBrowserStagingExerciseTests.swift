// READY scaffold only — the executable AppModel test suite is absent.
//
// Required deterministic coverage: active current found/incomplete/partial/stale
// and cached retryable/required-update admission; every waiting/absence/unavailable/
// uncached/mismatched/archived/stale-selection refusal with zero calls; exact
// Account/Project/revision confirmation capture; cancel zero-call; exact unchanged
// confirmation admission; selection/lifecycle/revision change invalidation;
// Operation/actor/version/time forwarding; simultaneous confirm one-call behavior;
// ambiguous retry byte identity; all six local operation
// states; immediate Active-to-Archived browser movement without false ready/empty;
// applied archived persistence; rejected/conflict Active restoration; refreshed-
// revision/new-ID explicit recovery; selection change; observation throw,
// cancellation and unexpected completion before/after evidence; stop/restart and
// noncooperative late-value generation isolation; and bounded non-secret errors.
