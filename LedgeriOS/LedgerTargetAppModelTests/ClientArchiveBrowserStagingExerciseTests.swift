// READY scaffold only — the executable AppModel test suite is absent.
//
// Required deterministic coverage: currently observed active found/incomplete/
// partial/stale and cached retryable/required-update admission; every waiting/
// absence/unavailable/uncached/mismatched/archived/stale-selection refusal with
// zero calls; exact Account/Client/revision action capture; Operation/actor/
// version/time forwarding; simultaneous one-call behavior; byte-identical
// ambiguous retry; all six local operation states; immediate Active-to-Archived
// browser movement without false ready/empty; shared Project Setup Client-option
// removal; stale and current new/unaccepted post-archive Project-creation refusal;
// exact replay of a pre-archive accepted Project setup; applied archived
// persistence; rejected/conflict revelation of exact underlying evidence without
// forcing Active; refreshed-current-active revision/new-ID explicit recovery;
// observation throw/cancellation/completion; stop/
// restart/noncooperative late-value drainage; and bounded non-secret diagnostics.
