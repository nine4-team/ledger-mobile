// READY scaffold only — executable AppModel tests are absent.
//
// Planned executable presenter/AppModel tests cover exact Project and Business
// Inventory requests, every local readiness/empty/failure state, deterministic
// rows, represented stable-ID selection, rapid request replacement with a
// delayed noncooperative old stream, cancellation and stop/join behavior. They
// prove zero operation calls and no Item or Space mutation surface; they do not
// freeze unspecified production selection-retention policy.
