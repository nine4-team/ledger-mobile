// DRAFT scaffold only: no executable Space-browser AppModel tests exist here.
//
// A later READY implementation must exhaustively prove list presentation for
// waiting, partial, stale, ready nonempty, authoritative empty, and bounded
// failure; active-only presentation; stable duplicate-name ordering;
// represented-ID-only selection; exact selection-
// to-SpaceCoreDetailsRequest derivation; deselection/scope replacement; and
// refusal of late noncooperative list or detail emissions after cancellation.
// It must also prove that unknown Item count is omitted, archived list UI is not
// invented, search remains absent, the AppModel exposes no provider-specific
// surface, and the model
// offers no create, edit, archive, assignment, checklist, Item, media, accounting,
// MCP, source-backend, hosted, production, migration, or cutover action.
