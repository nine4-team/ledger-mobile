// Frozen executable obligations for direct-space-creation-use-case-contracts.
//
// Tests must independently prove raw-input preservation and canonical intent,
// both exact scopes, duplicate-name validity, exact command assembly, one port
// invocation, receipt validation, validation/port failure propagation, stable
// diagnostics, conversion of the current Space validation responsibility, and
// permanent scope exclusions.
//
// Do not treat transient form input, an in-memory test port, or a queued receipt
// as physical persistence, current authorization, server application, visible
// optimistic state, authenticated Sync, migration or production proof.
