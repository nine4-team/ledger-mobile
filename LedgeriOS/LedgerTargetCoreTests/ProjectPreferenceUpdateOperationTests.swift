// Target-only Phase 1 verification scaffold for UpdateProjectPreferences.
// The ready gate must trace and freeze exact own-Principal scope, ordered pin
// replacement, absent-versus-revision preconditions, canonical restart,
// tamper/replay refusal, and no-false-receipt port behavior before executable
// tests are implemented. It must not claim authentication, category validity,
// budget effects, physical offline durability, provider/schema/RLS/Sync,
// app/MCP, migration, hosted-resource, or production evidence.
