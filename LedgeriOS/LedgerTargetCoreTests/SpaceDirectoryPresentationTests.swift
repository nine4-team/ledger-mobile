// DRAFT SCAFFOLD ONLY — no executable tests.
//
// Independent READY review must freeze the adjacent dossier before this leaf
// imports LedgerTargetCore or adds declarations/assertions.
//
// Planned focused matrix:
// 1. Exact Project and Business Inventory Account/scope isolation; cross-Account
//    and cross-scope rows reject without disclosure.
// 2. Active-only projection excludes explicit archived rows, preserves duplicate
//    names by stable identity and canonicalizes folded-name/exact-name/ID order.
// 3. Checklist progress derives from validated ordered checklist state, including
//    zero checklists, zero-item checklists and nonzero checked/total fixtures.
// 4. Item-count presentation is explicitly unavailable for every row and cannot
//    render or encode a fabricated numeric zero.
// 5. Ready + complete + source-exhaustive empty is authoritative; ready
//    incomplete/nonexhaustive, partial, stale and failed empty remain unknown.
//    Partial/stale represented active rows preserve their exact quality.
// 6. Canonical request/source/presentation/selection restart is byte-identical;
//    every field, order, count, timestamp, fingerprint and encoded-key tamper
//    fails through one unique bounded provider-neutral diagnostic.
// 7. Selection rejects absent, archived, wrong-scope and same-name sibling IDs,
//    plus stale/rebound presentation evidence, before producing only the exact
//    existing SpaceCoreDetailsRequest.
// 8. Detail validation accepts separately valid mutable detail/revision changes
//    but rejects selected Account, Space or immutable-scope rebinding.
// 9. Reference-port waiting/failure/cached/rebound/cancellation cases emit no
//    false row, false empty, false selection, detail or authorization evidence.
// 10. Static API/key allowlists prove no Item, media, search, archive command,
//     archived browser, route, provider, persistence, accounting or production
//     surface entered the boundary.
