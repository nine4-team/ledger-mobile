// READY scaffold only: no executable application model exists here yet.
//
// The bounded implementation will observe one exact source Project through the
// verified destination-selection contract, expose waiting/partial/stale/ready/
// authoritative-empty/failure truth, and accept selection only for a currently
// represented stable destination ID. Source disappearance or relationship
// change clears prior choices before new evidence. It owns no Item choice,
// amount, command, confirmation, accounting, or infrastructure behavior.
