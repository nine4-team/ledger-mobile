// READY TEST SCAFFOLD ONLY — contains no executable test or passing assertion.
//
// Future tests must prove exact tagged-value boundaries; golden domain-separated
// length-prefixed bundle preimages and digests including canonical manifest
// content and excluding only final envelope framing and digest bytes; exact
// payload/file/entity counts and hashes; source-fixture snapshot parity; logical
// path, tamper, size, version, duplicate, unknown-field and noncanonical refusal;
// exact reviewed synthetic catalog digests, schema-specific prohibited fields
// and human byte-review evidence without claiming generic PII detection; and
// explicit malformed, unsupported, ambiguous, orphan, cross-Account and media-
// metadata cases without opening Firebase, a file, provider, database or network.
