// READY TEST SCAFFOLD ONLY — contains no executable test or passing assertion.
//
// Future tests must prove the exact source_fixture/targetLocal/dry_run/no-
// credential tuple, default-to-dry-run behavior, canonical plan revalidation,
// exact target/Account/artifact/contract binding, stable non-secret failures,
// schema-v1 canonical request/policy/receipt bytes, domain-separated golden
// digests, raw/canonical size ceilings and unknown-field refusal; and refusal of
// every mismatch, apply/production/hosted mode, credential,
// provider default, environment fallback, or all-Account request without a
// token. Public shape proof must show the primitive accepts only immutable
// provider-free values and exposes no source, target, filesystem, database,
// provider, credential or network callback. Package-interface proof must keep
// the token non-public, non-Codable and private-initializer while available to a
// later same-package executor; its trusted-policy ordering remains separate.
