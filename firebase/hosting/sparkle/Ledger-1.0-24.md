# Ledger 1.0 (24)

- Adds an optional save-for-next-time toggle when entering a custom transaction vendor/source.
- Keeps one-off sources from cluttering vendor presets unless explicitly saved.
- Improves scrolling so the save toggle remains visible while typing.
