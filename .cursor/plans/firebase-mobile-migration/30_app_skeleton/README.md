# App skeleton (`30_app_skeleton/`)

Specs for the reusable “starter app” foundation that all features sit on.

Put docs here like:
- `app_skeleton_spec.md` (auth bootstrap, nav shell, local DB init/migrations, sync wiring, logging, error boundaries, offline banners, retry patterns)

