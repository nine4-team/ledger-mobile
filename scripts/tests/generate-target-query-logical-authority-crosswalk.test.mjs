// READY scaffold only. Do not add executable tests before the immutable
// target-query-logical-authority-crosswalk-control READY checkpoint passes.
//
// The implementation suite must prove exact 18/18 TQUERY set equality,
// inventory-digest and signature binding, closed-schema/discriminated-axis
// validation, exact enum/reference allowlists, stable collision-checked TACCESS
// identity, separate canonical-row mapping-hash drift, deterministic JSON
// bytes, missing/stale artifact refusal, read-only check mode, exact lifecycle
// allowlists, and global physical-plane deferral. Tests must prove structural
// validation only and must not pretend to infer semantics from free text or
// Markdown. The separate 386-source-query reconciliation remains out of scope.
// The implementation negative matrix must include the contiguous forbidden keys
// firestoreQuery and firestoreIndex. Their presence here deliberately proves
// this control leaf is excluded from Firebase audit/repair discovery by path.
