// READY CONTRACT — TARGET QUERY-PORT INVENTORY GENERATOR TESTS
//
// This leaf is intentionally comment-only until its exact READY commit passes
// the complete conversion gate. Implementation may replace only this comment.
//
// The future dependency-free Node suite must prove:
//
// 1. The repository baseline contains exactly 16 verified owner surfaces,
//    16 public protocol names ending exactly `Querying`, and 18 direct method
//    requirements; every current selector begins with `watch`, and the exact
//    two multi-method protocols retain their exact method sets.
// 2. JSON and Markdown bytes, row ordering, stable TQUERY IDs and signature
//    hashes are deterministic across repeated runs and across CRLF, harmless
//    whitespace and comment/string-content normalization.
// 3. Added, removed or renamed Querying protocols/methods and every signature
//    axis—labels, parameter/return types, generics, attributes, async, throws
//    and where clauses—make tracked artifacts stale until explicitly reviewed.
// 4. Multiline requirements, attributes, nested generic types and where clauses
//    parse exactly, while comments, strings, implementations, extensions,
//    private/internal protocols and non-Querying protocols are excluded.
// 5. Unsupported direct functions, malformed or ambiguous declarations,
//    duplicate selectors/overloads, empty Querying protocols, missing or
//    ambiguous manifest ownership and invalid owner status all fail closed.
// 6. Missing either generated artifact fails check; check never writes;
//    generate followed by check is clean and byte-identical.
// 7. The READY checkpoint changes only these two comment scaffolds plus the
//    dossier/evidence/control metadata and the deliberately reviewed conversion
//    discoverer. The implementation checkpoint may replace only these two
//    leaves and add the exact two generated artifacts, package scripts, Ubuntu
//    workflow hook and target-environment job dependency frozen by the dossier.
//    Promotion docs remain separate.
// 8. Existing 16 query-owner surfaces, package/workflow support surfaces,
//    FILE-063B0E6EC659, MAN-INDEX-001 and FILE-208B7E9D7F47 retain their exact
//    prior lifecycle status and ownership. This control does not claim logical
//    or physical index design, the 386 QUERY-* source occurrences, any runtime
//    Swift manifest, or any product/provider/hosted/production behavior.
