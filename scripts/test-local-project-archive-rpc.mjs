// READY scaffold only — this file makes no request and imports no client.
//
// The executable local-only Data API runner must use synthetic authenticated
// user tokens and the publishable local key, never service-role credentials or
// hosted endpoints. It must prove exact project-archive-v1 command bytes/hash,
// authenticated Account/Principal derivation, apply, exact and changed replay,
// lost-response retry, true same-revision concurrency, revision conflicts and
// exhaustion, positive/negative scope/capability matrices, result validation,
// bounded diagnostics and exact Project/relationship/child/history preservation.
