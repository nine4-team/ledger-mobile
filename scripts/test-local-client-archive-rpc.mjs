// READY scaffold only — this file makes no request and imports no client.
//
// The executable local-only Data API runner must use synthetic authenticated user
// tokens and the publishable local key, never service-role credentials or hosted
// endpoints. It must prove exact client-archive-v1 bytes/hash, account-bound
// operation identity, authenticated Account/Principal derivation, apply, exact/
// changed/lost-response replay, true archive/archive and archive/create-Project
// concurrency, durable project_setup_client_not_selectable rather than raw failure,
// cross-Account/cross-command result refusal and non-archive namespace squatting,
// revision conflicts/exhaustion, positive/negative scope/capability
// matrices, result validation, bounded diagnostics and exact preservation of every
// Project relationship and represented Client/history/accounting field.
